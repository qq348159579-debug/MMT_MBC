## 未完成需求清单（截至：2026-05-14）

### 1) 站内通知中心（前端 UI 未完成）
- 左上角铃铛入口：需要放到左侧栏顶部（Logo 区域同一行/左上角），显示未读红点/数字。
- 通知列表 UI：已读/未读样式、加载态、空态（暂无通知）。
- 通知详情：点击后可展开查看事件明细 events（每次变更的 before/after、原因、时间、操作人）。
- “全部已读”按钮（前端接入后端 `/api/notifications/mark-all-read`）。
- 只做“未读数”强提示：已按你的要求不弹 toast、不强弹窗。

### 2) 通知跳转（从通知定位到采购行）未完成
- 点击通知后跳转到采购管理，并自动：
  - 选择项目（projectId）
  - 选择 BOM 清单（bomType + bomId）
  - 定位/高亮对应采购行（requirementId）
- 需要在采购管理页增加 “按 id 定位并滚动高亮” 的逻辑（例如在表格加载后执行 scrollTo + 临时高亮）。

### 3) 交期/ETA 修改强制原因：前端交互未完成（后端已强制）
- 后端已在 `/api/material-requirements/patch` 强制：修改 deliveryTime/expectedArrival 时必须填写 `changeReason`，否则 400 报错。
- 前端需要在用户修改交期/ETA 并保存时：
  - 弹出“变更原因（必填）”输入框
  - 把输入写入该行 `changeReason`（可选写入 `changeReasonNote`）
  - 再执行 patch 保存

### 4) 通知“列负责人”规则需要补全 expectedArrival 映射（部分已做）
- 目前收件人计算按列负责人（materialTrackingTemplate.assigneeIds）：
  - status → status 列负责人
  - deliveryTime → deliveryTime 列负责人
  - expectedArrival：目前后端暂时映射到 deliveryTime 列负责人（MVP 方案）
- 若你们后续希望 ETA 列独立负责人，需要在模板中新增/启用 expectedArrival 列配置并按该列取负责人。

### 5) SQL Server 通知表的运维与清理策略（待做）
- 你选择“最近 200 条”，目前后端列表接口已限制 top=200。
- 但数据库表会持续增长：需要定时清理策略（例如保留最近 N 天/最近 N 条事件）。

